cmake_minimum_required (VERSION 3.31)
message(STATUS "Configuring PonyEngine")

if(PROJECT_NAME)
	message(VERBOSE "Configuring as sub-project")
	set(PONY_IS_SUBPROJECT TRUE)
else()
	message(VERBOSE "Configuring as main project")
	set(PONY_IS_SUBPROJECT FALSE)
endif()

if(NOT PONY_IS_SUBPROJECT)
	message(VERBOSE "Configuring environment")
	set(CMAKE_CXX_STANDARD 23)
	set(CMAKE_CXX_STANDARD_REQUIRED ON)
	set(CMAKE_EXPERIMENTAL_CXX_IMPORT_STD "0e5b6991-d74f-4b3d-a41c-cf096e0b2508")
	set(CMAKE_CXX_MODULE_STD ON)
endif()
include(FetchContent)
include("CMake/Project.cmake")

message(VERBOSE "Configuring project")
project("PonyEngine" VERSION "0.0.0.0" LANGUAGES CXX)
if(NOT PONY_IS_SUBPROJECT)
	pony_clean_build_options()
endif()

message(VERBOSE "Configuring platform: ${CMAKE_SYSTEM_NAME}")
if(PONY_WINCORE OR PONY_WINDOWS)
	message(FATAL_ERROR "Platform defines are preset")
endif()
if(CMAKE_SYSTEM_NAME STREQUAL "Windows")
	if(CMAKE_SYSTEM_VERSION VERSION_LESS "10.0.26100.0")
		message(FATAL_ERROR "Building with Windows SDK '${CMAKE_SYSTEM_VERSION}', but 10.0.26100.0 or newer is required")
	endif()
	if(NOT PONY_IS_SUBPROJECT)
		if(NOT CMAKE_SYSTEM_VERSION VERSION_EQUAL "10.0.26100.0")
			message(FATAL_ERROR "Building with Windows SDK '${CMAKE_SYSTEM_VERSION}', but 10.0.26100.0 is required for engine build")
		endif()
	endif()

	set(PONY_WINCORE "TRUE")
	set(PONY_WINDOWS "TRUE")

	if(PONY_IS_SUBPROJECT)
		set(PONY_WINCORE "TRUE" PARENT_SCOPE)
		set(PONY_WINDOWS "TRUE" PARENT_SCOPE)
	endif()
else()
	message(FATAL_ERROR "Unsupported platform!")
endif()

message(VERBOSE "Validating settings")
if(NOT PONY_COMPANY_NAME)
	message(FATAL_ERROR "PONY_COMPANY_NAME isn't set")
endif()
if(NOT PONY_PROJECT_NAME)
	message(FATAL_ERROR "PONY_PROJECT_NAME isn't set")
endif()
if(NOT PONY_PROJECT_VERSION)
	message(FATAL_ERROR "PONY_PROJECT_VERSION isn't set")
endif()
if(NOT PONY_COMPANY_TITLE)
	message(FATAL_ERROR "PONY_COMPANY_TITLE isn't set")
endif()
if(NOT PONY_PROJECT_TITLE)
	message(FATAL_ERROR "PONY_PROJECT_TITLE isn't set")
endif()

message(VERBOSE "Configuring modules")
set(PONY_BASE_MODULES "")
set(PONY_LOG_MODULES "")
set(PONY_ENGINE_MODULES "")
set(PONY_GAME_MODULES "")

add_subdirectory("Engine/Core")
if(NOT PONY_IS_SUBPROJECT)
	message(VERBOSE "Post-configuring")
	pony_set_log_defines(PonyEngine.Core ${PONY_ENGINE_LOG_LEVEL} ${PONY_ENGINE_LOG_STACKTRACE_LEVEL} ${PONY_CONSOLE_LOG} ${PONY_PLATFORM_CONSOLE_LOG})
	pony_set_build_options(PonyEngine.Core ${PONY_ENGINE_OPTIMIZATION})
endif()

if(PONY_ENGINE_PATH)
	add_subdirectory("Engine/Path")
	if(NOT PONY_IS_SUBPROJECT)
		message(VERBOSE "Post-configuring")
		pony_set_log_defines(PonyEngine.Path ${PONY_ENGINE_LOG_LEVEL} ${PONY_ENGINE_LOG_STACKTRACE_LEVEL} ${PONY_CONSOLE_LOG} ${PONY_PLATFORM_CONSOLE_LOG})
		pony_set_build_options(PonyEngine.Path ${PONY_ENGINE_OPTIMIZATION})
	endif()
endif()

if(PONY_ENGINE_PATH_EXTENSION)
	add_subdirectory("Engine/Path.Extension")
	if(NOT PONY_IS_SUBPROJECT)
		message(VERBOSE "Post-configuring")
		pony_set_log_defines(PonyEngine.Path.Extension ${PONY_ENGINE_LOG_LEVEL} ${PONY_ENGINE_LOG_STACKTRACE_LEVEL} ${PONY_CONSOLE_LOG} ${PONY_PLATFORM_CONSOLE_LOG})
		pony_set_build_options(PonyEngine.Path.Extension ${PONY_ENGINE_OPTIMIZATION})
	endif()
endif()

if(PONY_ENGINE_PATH_CONFIGURATOR)
	add_subdirectory("Engine/Path.Configurator")
	if(NOT PONY_IS_SUBPROJECT)
		message(VERBOSE "Post-configuring")
		pony_set_log_defines(PonyEngine.Path.Configurator ${PONY_ENGINE_LOG_LEVEL} ${PONY_ENGINE_LOG_STACKTRACE_LEVEL} ${PONY_CONSOLE_LOG} ${PONY_PLATFORM_CONSOLE_LOG})
		pony_set_build_options(PonyEngine.Path.Configurator ${PONY_ENGINE_OPTIMIZATION})
		list(APPEND PONY_BASE_MODULES PonyEngine.Path.Configurator)
	endif()
endif()

if(PONY_ENGINE_PATH_MAIN)
	add_subdirectory("Engine/Path.Main")
	if(NOT PONY_IS_SUBPROJECT)
		message(VERBOSE "Post-configuring")
		pony_set_log_defines(PonyEngine.Path.Main ${PONY_ENGINE_LOG_LEVEL} ${PONY_ENGINE_LOG_STACKTRACE_LEVEL} ${PONY_CONSOLE_LOG} ${PONY_PLATFORM_CONSOLE_LOG})
		pony_set_build_options(PonyEngine.Path.Main ${PONY_ENGINE_OPTIMIZATION})
		list(APPEND PONY_BASE_MODULES PonyEngine.Path.Main)
	endif()
endif()

if(PONY_ENGINE_SURFACE)
	add_subdirectory("Engine/Surface")
	if(NOT PONY_IS_SUBPROJECT)
		message(VERBOSE "Post-configuring")
		pony_set_log_defines(PonyEngine.Surface ${PONY_ENGINE_LOG_LEVEL} ${PONY_ENGINE_LOG_STACKTRACE_LEVEL} ${PONY_CONSOLE_LOG} ${PONY_PLATFORM_CONSOLE_LOG})
		pony_set_build_options(PonyEngine.Surface ${PONY_ENGINE_OPTIMIZATION})
	endif()
endif()

if(PONY_ENGINE_SURFACE_EXTENSION)
	add_subdirectory("Engine/Surface.Extension")
	if(NOT PONY_IS_SUBPROJECT)
		message(VERBOSE "Post-configuring")
		pony_set_log_defines(PonyEngine.Surface.Extension ${PONY_ENGINE_LOG_LEVEL} ${PONY_ENGINE_LOG_STACKTRACE_LEVEL} ${PONY_CONSOLE_LOG} ${PONY_PLATFORM_CONSOLE_LOG})
		pony_set_build_options(PonyEngine.Surface.Extension ${PONY_ENGINE_OPTIMIZATION})
	endif()
endif()

if(PONY_ENGINE_SURFACE_CONFIGURATOR)
	add_subdirectory("Engine/Surface.Configurator")
	if(NOT PONY_IS_SUBPROJECT)
		message(VERBOSE "Post-configuring")
		pony_set_log_defines(PonyEngine.Surface.Configurator ${PONY_ENGINE_LOG_LEVEL} ${PONY_ENGINE_LOG_STACKTRACE_LEVEL} ${PONY_CONSOLE_LOG} ${PONY_PLATFORM_CONSOLE_LOG})
		pony_set_build_options(PonyEngine.Surface.Configurator ${PONY_ENGINE_OPTIMIZATION})
		list(APPEND PONY_BASE_MODULES PonyEngine.Surface.Configurator)
	endif()
endif()

if(PONY_ENGINE_SURFACE_MAIN)
	add_subdirectory("Engine/Surface.Main")
	if(NOT PONY_IS_SUBPROJECT)
		message(VERBOSE "Post-configuring")
		pony_set_log_defines(PonyEngine.Surface.Main ${PONY_ENGINE_LOG_LEVEL} ${PONY_ENGINE_LOG_STACKTRACE_LEVEL} ${PONY_CONSOLE_LOG} ${PONY_PLATFORM_CONSOLE_LOG})
		pony_set_build_options(PonyEngine.Surface.Main ${PONY_ENGINE_OPTIMIZATION})
		list(APPEND PONY_BASE_MODULES PonyEngine.Surface.Main)
	endif()
endif()

if(PONY_ENGINE_LOG_EXTENSION)
	add_subdirectory("Engine/Log.Extension")
	if(NOT PONY_IS_SUBPROJECT)
		message(VERBOSE "Post-configuring")
		pony_set_log_defines(PonyEngine.Log.Extension ${PONY_ENGINE_LOG_LEVEL} ${PONY_ENGINE_LOG_STACKTRACE_LEVEL} ${PONY_CONSOLE_LOG} ${PONY_PLATFORM_CONSOLE_LOG})
		pony_set_build_options(PonyEngine.Log.Extension ${PONY_ENGINE_OPTIMIZATION})
	endif()
endif()

if(PONY_ENGINE_LOG_MAIN)
	add_subdirectory("Engine/Log.Main")
	if(NOT PONY_IS_SUBPROJECT)
		message(VERBOSE "Post-configuring")
		pony_set_log_defines(PonyEngine.Log.Main ${PONY_ENGINE_LOG_LEVEL} ${PONY_ENGINE_LOG_STACKTRACE_LEVEL} ${PONY_CONSOLE_LOG} ${PONY_PLATFORM_CONSOLE_LOG})
		pony_set_build_options(PonyEngine.Log.Main ${PONY_ENGINE_OPTIMIZATION})
		list(APPEND PONY_LOG_MODULES PonyEngine.Log.Main)
	endif()
endif()

if(PONY_ENGINE_LOG_CONSOLE)
	add_subdirectory("Engine/Log.Console")
	if(NOT PONY_IS_SUBPROJECT)
		message(VERBOSE "Post-configuring")
		pony_set_log_defines(PonyEngine.Log.Console ${PONY_ENGINE_LOG_LEVEL} ${PONY_ENGINE_LOG_STACKTRACE_LEVEL} ${PONY_CONSOLE_LOG} ${PONY_PLATFORM_CONSOLE_LOG})
		pony_set_build_options(PonyEngine.Log.Console ${PONY_ENGINE_OPTIMIZATION})
		list(APPEND PONY_LOG_MODULES PonyEngine.Log.Console)
	endif()
endif()

if(PONY_ENGINE_LOG_PLATFORM_CONSOLE)
	add_subdirectory("Engine/Log.PConsole")
	if(NOT PONY_IS_SUBPROJECT)
		message(VERBOSE "Post-configuring")
		pony_set_log_defines(PonyEngine.Log.PlatformConsole ${PONY_ENGINE_LOG_LEVEL} ${PONY_ENGINE_LOG_STACKTRACE_LEVEL} ${PONY_CONSOLE_LOG} ${PONY_PLATFORM_CONSOLE_LOG})
		pony_set_build_options(PonyEngine.Log.PlatformConsole ${PONY_ENGINE_OPTIMIZATION})
		list(APPEND PONY_LOG_MODULES PonyEngine.Log.PlatformConsole)
	endif()
endif()

if(PONY_ENGINE_LOG_FILE)
	add_subdirectory("Engine/Log.File")
	if(NOT PONY_IS_SUBPROJECT)
		message(VERBOSE "Post-configuring")
		pony_set_log_defines(PonyEngine.Log.File ${PONY_ENGINE_LOG_LEVEL} ${PONY_ENGINE_LOG_STACKTRACE_LEVEL} ${PONY_CONSOLE_LOG} ${PONY_PLATFORM_CONSOLE_LOG})
		pony_set_build_options(PonyEngine.Log.File ${PONY_ENGINE_OPTIMIZATION})
		list(APPEND PONY_LOG_MODULES PonyEngine.Log.File)
	endif()
endif()

if(PONY_ENGINE_ENGINE_EXTENSION)
	add_subdirectory("Engine/Engine.Extension")
	if(NOT PONY_IS_SUBPROJECT)
		message(VERBOSE "Post-configuring")
		pony_set_log_defines(PonyEngine.Engine.Extension ${PONY_ENGINE_LOG_LEVEL} ${PONY_ENGINE_LOG_STACKTRACE_LEVEL} ${PONY_CONSOLE_LOG} ${PONY_PLATFORM_CONSOLE_LOG})
		pony_set_build_options(PonyEngine.Engine.Extension ${PONY_ENGINE_OPTIMIZATION})
	endif()
endif()

if(PONY_ENGINE_ENGINE_MAIN)
	add_subdirectory("Engine/Engine.Main")
	if(NOT PONY_IS_SUBPROJECT)
		message(VERBOSE "Post-configuring")
		pony_set_log_defines(PonyEngine.Engine.Main ${PONY_ENGINE_LOG_LEVEL} ${PONY_ENGINE_LOG_STACKTRACE_LEVEL} ${PONY_CONSOLE_LOG} ${PONY_PLATFORM_CONSOLE_LOG})
		pony_set_build_options(PonyEngine.Engine.Main ${PONY_ENGINE_OPTIMIZATION})
		list(APPEND PONY_ENGINE_MODULES PonyEngine.Engine.Main)
	endif()
endif()

if(PONY_ENGINE_APPLICATION_MAIN)
	add_subdirectory("Engine/Application.Main")
	if(NOT PONY_IS_SUBPROJECT)
		message(VERBOSE "Post-configuring")
		pony_set_log_defines(PonyEngine.Application.Main ${PONY_ENGINE_LOG_LEVEL} ${PONY_ENGINE_LOG_STACKTRACE_LEVEL} ${PONY_CONSOLE_LOG} ${PONY_PLATFORM_CONSOLE_LOG})
		pony_set_build_options(PonyEngine.Application.Main ${PONY_ENGINE_OPTIMIZATION})
	endif()
endif()

if(NOT PONY_IS_SUBPROJECT AND PONY_ENGINE_ADD_GAME)
	message(VERBOSE "Adding game")
	add_subdirectory("Game/${PONY_ENGINE_GAME_NAME}/GameSample")
	message(VERBOSE "Post-configuring")
	pony_set_log_defines(GameSample ${PONY_GAME_LOG_LEVEL} ${PONY_GAME_LOG_STACKTRACE_LEVEL} ${PONY_CONSOLE_LOG} ${PONY_PLATFORM_CONSOLE_LOG})
	pony_set_build_options(GameSample ${PONY_GAME_OPTIMIZATION})
	list(APPEND PONY_GAME_MODULES GameSample)
endif()

if(NOT PONY_IS_SUBPROJECT)
	set(PONY_INSTALL_TARGETS "")
	if(PONY_MAKE_GROUPS)
		message(VERBOSE "Making application module groups")

		if(PONY_BASE_MODULES)
			pony_make_module_group(PONY_BASE_MODULES PonyEngine.Base.Group "PonyEngine.Base" 23)
			pony_set_log_defines(PonyEngine.Base.Group ${PONY_ENGINE_LOG_LEVEL} ${PONY_ENGINE_LOG_STACKTRACE_LEVEL} ${PONY_CONSOLE_LOG} ${PONY_PLATFORM_CONSOLE_LOG})
			pony_set_build_options(PonyEngine.Base.Group ${PONY_ENGINE_OPTIMIZATION})
			if(TARGET PonyEngine.Application.Main)
				target_link_libraries(PonyEngine.Application.Main PRIVATE PonyEngine.Base.Group)
			endif()
			list(APPEND PONY_INSTALL_TARGETS PonyEngine.Base.Group)
		endif()

		if(PONY_LOG_MODULES)
			pony_make_module_group(PONY_LOG_MODULES PonyEngine.Log.Group "PonyEngine.Log" 23)
			pony_set_log_defines(PonyEngine.Log.Group ${PONY_ENGINE_LOG_LEVEL} ${PONY_ENGINE_LOG_STACKTRACE_LEVEL} ${PONY_CONSOLE_LOG} ${PONY_PLATFORM_CONSOLE_LOG})
			pony_set_build_options(PonyEngine.Log.Group ${PONY_ENGINE_OPTIMIZATION})
			if(TARGET PonyEngine.Application.Main)
				target_link_libraries(PonyEngine.Application.Main PRIVATE PonyEngine.Log.Group)
			endif()
			list(APPEND PONY_INSTALL_TARGETS PonyEngine.Log.Group)
		endif()

		if(PONY_ENGINE_MODULES)
			pony_make_module_group(PONY_ENGINE_MODULES PonyEngine.Engine.Group "PonyEngine.Engine" 23)
			pony_set_log_defines(PonyEngine.Engine.Group ${PONY_ENGINE_LOG_LEVEL} ${PONY_ENGINE_LOG_STACKTRACE_LEVEL} ${PONY_CONSOLE_LOG} ${PONY_PLATFORM_CONSOLE_LOG})
			pony_set_build_options(PonyEngine.Engine.Group ${PONY_ENGINE_OPTIMIZATION})
			if(TARGET PonyEngine.Application.Main)
				target_link_libraries(PonyEngine.Application.Main PRIVATE PonyEngine.Engine.Group)
			endif()
			list(APPEND PONY_INSTALL_TARGETS PonyEngine.Engine.Group)
		endif()

		if(PONY_GAME_MODULES)
			pony_make_module_group(PONY_GAME_MODULES PonyEngine.Game.Group "PonyEngine.Game" 23)
			pony_set_log_defines(PonyEngine.Game.Group ${PONY_GAME_LOG_LEVEL} ${PONY_GAME_LOG_STACKTRACE_LEVEL} ${PONY_CONSOLE_LOG} ${PONY_PLATFORM_CONSOLE_LOG})
			pony_set_build_options(PonyEngine.Game.Group ${PONY_GAME_OPTIMIZATION})
			if(TARGET PonyEngine.Application.Main)
				target_link_libraries(PonyEngine.Application.Main PRIVATE PonyEngine.Game.Group)
			endif()
			list(APPEND PONY_INSTALL_TARGETS PonyEngine.Game.Group)
		endif()
	else()
		message(VERBOSE "Adding application modules")

		if(TARGET PonyEngine.Application.Main)
			if(PONY_BASE_MODULES)
				target_link_libraries(PonyEngine.Application.Main PRIVATE ${PONY_BASE_MODULES})
			endif()
			if(PONY_LOG_MODULES)
				target_link_libraries(PonyEngine.Application.Main PRIVATE ${PONY_LOG_MODULES})
			endif()
			if(PONY_ENGINE_MODULES)
				target_link_libraries(PonyEngine.Application.Main PRIVATE ${PONY_ENGINE_MODULES})
			endif()
			if(PONY_GAME_MODULES)
				target_link_libraries(PonyEngine.Application.Main PRIVATE ${PONY_GAME_MODULES})
			endif()
		endif()
	endif()

	if(TARGET PonyEngine.Application.Main)
		list(APPEND PONY_INSTALL_TARGETS PonyEngine.Application.Main)
	endif()

	message(VERBOSE "Installing")
	install(TARGETS ${PONY_INSTALL_TARGETS}
		RUNTIME DESTINATION .
		LIBRARY DESTINATION
	)
endif()

if(NOT PONY_IS_SUBPROJECT AND PONY_ENGINE_TESTING)
	message(STATUS "Configuring test project")
	enable_testing()
	add_subdirectory("Tests")
endif()
